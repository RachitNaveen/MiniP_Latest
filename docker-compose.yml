version: '3'

services:
  securechat:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=production
